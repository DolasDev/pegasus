# This AWS SAM template has been generated from your function's configuration.
# If your function has one or more triggers, note that the AWS resources
# associated with these triggers aren't fully specified in this template and
# include placeholder values. Open this template in AWS Infrastructure Composer
# or your favorite IDE and modify it to specify a serverless application with
# other AWS resources.
#
# - SAM automatically generates permissions to interact with the resources
#   defined in this template. If your function needs additional permissions, add
#   Policies to the AWS::Serverless::Function resource.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model (SAM) template describing your function.
Resources:
  pegasusapiStoreRequest2EventsQueue:
    Type: AWS::Serverless::Function
    Properties:
      Architectures:
        - x86_64
      CodeUri: ./src
      Description: >-
        A simple backend (read/write to DynamoDB) with a RESTful API endpoint
        using Amazon API Gateway.
      EphemeralStorage:
        Size: 512
      Handler: lambda_function.lambda_handler
      LoggingConfig:
        LogFormat: Text
        LogGroup: /aws/lambda/pegasus_api_StoreRequest2EventsQueue
      MemorySize: 512
      PackageType: Zip
      RecursiveLoop: Terminate
      Runtime: python3.10
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
      SnapStart:
        ApplyOn: None
      Tags:
        lambda-console:blueprint: microservice-http-endpoint-python
      Timeout: 10
      Tracing: PassThrough
      Events:
        Trigger1Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /orders/create
        Trigger2Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /orders
        Trigger3Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /MyResource
        Trigger4Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /orders/create/{customer_app_id}
        Trigger5Event:
          Type: HttpApi
          Properties:
            Method: ANY
            Path: /MyResource
