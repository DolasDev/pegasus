# Fold CI-plan Phase 4 into the AI plan and archive `audit-ci-pipeline-efficiency.md`

> **Status: COMPLETE** — 2026-08-11. Branch `chore/fold-ci-phase4`.
>
> **Deviation:** the fold turned out to be pure **deduplication** — the AI plan's
> Phase 3 already specified the same `ci-triage.yml` as a strict superset, so
> only one decision record (the "explicit non-recommendation") needed carrying.
> Also fixed a **pre-existing** dead link found by link-checking after the move:
> master-plan rank 8 → `audit-observability-alerting.md`, archived earlier
> without a sha prefix, so its relative link had been broken since then.
> Completes the archive condition the plan itself declared on 2026-08-08 after
> its Phase-2 targets were re-baselined and met (#602).

## Context

`audit-ci-pipeline-efficiency.md` has Phases 1-3 shipped and verified, and its
Phase-2 targets re-baselined against the current 7-10-job pipeline (#602). The
only thing keeping it open is **Phase 4 — `ci-triage.yml` AI failure triage**,
which is user-deferred under `audit-ai-process-automation.md`.

**Checked before moving anything: this is a DUPLICATE, not a gap.**
`audit-ai-process-automation.md` **Phase 3** already specifies the same
`.github/workflows/ci-triage.yml`, and is a strict superset of the CI plan's
Phase 4:

|                             | CI plan Phase 4 | AI plan Phase 3                                |
| --------------------------- | --------------- | ---------------------------------------------- |
| Trigger workflows           | `[CI]`          | `['CI', 'Deploy']`                             |
| Concurrency group           | —               | per-branch, cancel-in-progress                 |
| Known-failure matching      | —               | against `GOTCHAS.md` + CLAUDE.md CI/CD section |
| Comment target              | PR/commit       | commit, or the PR if one exists                |
| Model / turn cap            | —               | `--model claude-sonnet-4-6 --max-turns 12`     |
| Cancelled-vs-failure nuance | —               | documented                                     |

So the fold is **deduplication**: nothing needs transplanting except one
decision record the AI plan does not carry.

## Plan

- [x] **1. Carry the one unique item into `audit-ai-process-automation.md`
      Phase 3** — the CI plan's _"Explicit non-recommendation"_: Phases 1-3 of
      that plan were deterministic YAML where AI adds nothing, and AI-generated
      PR summaries / AI lint are explicitly rejected (the solo author already
      knows the diff; turbo + eslint are deterministic and faster). That is a
      scope-discipline record for the AI plan, which is exactly the plan most
      at risk of scope creep. Note the provenance.

- [x] **2. Record the fold in the CI plan before archiving it.** Mark Phase 4
      as MOVED with a pointer to AI Phase 3, so the archived file is honest
      about where its last open item went rather than looking abandoned.

- [x] **3. `git mv plans/in-progress/audit-ci-pipeline-efficiency.md
    plans/completed/<short-sha>-audit-ci-pipeline-efficiency.md`** per
      `dolas/agents/team/workflow.md` § Archiving Completed Plans.

- [x] **4. Fix the references in `audit-00-master-plan.md`** — it is the live
      index, and it contains a **relative markdown link**
      `[audit-ci-pipeline-efficiency.md](./audit-ci-pipeline-efficiency.md)` in
      the rank-2 row that breaks the moment the file moves. Repoint it, update
      the header note that says "archive-ready pending one call", and correct
      the still-open plan count in the 2026-07-30 cleanup-audit note.
      Historical Wave-1/2 checkbox references stay as-is — they are a record of
      what was done, not live links.

- [x] **5. Leave `plans/completed/*` references alone.** Archived plans are
      permanent records (`workflow.md`: "do not edit"); several cite
      `audit-ci-pipeline-efficiency.md:17-30` / `:266` by line number. Those are
      historical citations, correct as of when written.

## Files to Modify

| Action | File                                                                     |
| ------ | ------------------------------------------------------------------------ |
| Modify | `plans/in-progress/audit-ai-process-automation.md` (Phase 3 note)        |
| Move   | `plans/in-progress/audit-ci-pipeline-efficiency.md` → `plans/completed/` |
| Modify | `plans/in-progress/audit-00-master-plan.md` (link + counts)              |

## Side Effects & Risks

- **Archiving hides an unshipped item.** Mitigated: Phase 4's content is not
  lost — it is a duplicate of a live phase in a plan that remains open, and the
  archived file names where it went.
- **Broken relative link** if step 4 is skipped — the master plan is the live
  index, so a dead link there is the whole cost of a sloppy archive.
- **The AI plan stays deferred.** Nothing here starts AI work or changes its
  deferral; this only consolidates ownership.
- Plans-only diff; no code, no CI, nothing deployable.

## Acceptance Criteria / Verification

1. `plans/in-progress/audit-ci-pipeline-efficiency.md` no longer exists;
   `plans/completed/<sha>-audit-ci-pipeline-efficiency.md` does.
2. `grep -rn "](./audit-ci-pipeline-efficiency.md)" plans/in-progress/` → no
   hits (no dangling relative link).
3. The AI plan's Phase 3 carries the non-recommendation with its provenance.
4. The archived CI plan's Phase 4 is marked MOVED with a pointer.
5. The master plan's open-plan count matches reality.
