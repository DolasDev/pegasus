{
  "name": "@pegasus/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "eslint 'bin/**/*.ts' 'lib/**/*.ts'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "deploy:ci": "cdk deploy ${TARGET:---all} -c env=${ENV_NAME:-dev} --require-approval never --outputs-file /tmp/pegasus-cdk-outputs.json --app \"npx tsx bin/app.ts\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "aws-cdk": "^2.1139.0",
    "tsx": "^4.23.13",
    "typescript": "*",
    "vitest": "^4.1.11"
  }
}
