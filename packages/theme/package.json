{
  "name": "@pegasus/theme",
  "version": "0.1.0",
  "description": "Shared design tokens for Pegasus — colors, spacing, typography.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^4.0.0"
  },
  "private": true
}
