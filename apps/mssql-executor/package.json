{
  "name": "@pegasus/mssql-executor",
  "version": "0.0.0",
  "private": true,
  "description": "VPC Lambda that runs raw SQL against tenant MSSQL servers over the WireGuard tunnel.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mssql": "^12.7.0"
  },
  "devDependencies": {
    "@types/mssql": "^12.3.0",
    "@types/node": "^22.20.1",
    "typescript": "*",
    "vitest": "^4.1.5"
  }
}
