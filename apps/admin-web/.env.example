# Pegasus Admin Portal — Environment Variables
# Copy this file to .env.local and fill in values from CDK outputs / SSM.

# URL of the deployed Hono API (ApiStack output: PegasusApiUrl)
VITE_API_URL=http://localhost:3000

# Cognito Hosted UI domain (CognitoStack SSM: /pegasus/admin/cognito-hosted-ui-domain)
# Format: https://<domain>.auth.<region>.amazoncognito.com
VITE_COGNITO_DOMAIN=https://pegasus-000000000000.auth.us-east-1.amazoncognito.com

# Admin app client ID (CognitoStack SSM: /pegasus/admin/cognito-admin-client-id)
VITE_COGNITO_CLIENT_ID=

# OAuth 2.0 redirect URI — must match what is registered in the Cognito app client.
# For local dev:   http://localhost:5174/auth/callback
# For production:  https://admin.example.com/auth/callback
VITE_COGNITO_REDIRECT_URI=http://localhost:5174/auth/callback
