# Base URL of the Pegasus API (on-prem or cloud)
VITE_API_URL=http://localhost:3001

# Auth mode: "windows" (on-prem, SKIP_AUTH) or "api-key" (M2M)
VITE_AUTH_MODE=windows

# M2M API key — required when VITE_AUTH_MODE=api-key
VITE_LONGHAUL_API_KEY=
